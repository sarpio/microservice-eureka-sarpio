spring:
  application:
    name: USER-SERVICE
  cloud:
    config:
      uri: http://localhost:8888
      profile: "default"
      label: "main"
management:
  endpoints:
    web:
      exposure:
        include: health, info, refresh