server:
  port: 8888
spring:
  profiles:
    active: git
  application:
    name: "CONFIG-SERVER"
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sarpio/config-service.git
          clone-on-start: true
          default-label: main
